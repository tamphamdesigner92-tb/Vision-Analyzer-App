# Vision Analyzer - macOS (Apple Silicon), mot venv duy nhat.
#
# Ca hai model (Qwen2.5-VL-3B-Instruct bf16 va Qwen3-Reranker-4B) chay qua transformers, tren
# MPS, khong can CUDA. Nhanh Mac chi dung ban 3B: ban 7B-AWQ da bi bo (qua cham tren may 16GB),
# nen khong con can gptqmodel.
#
# CACH CAI:
#
#   python3 -m venv .venv
#   .venv/bin/python3 -m pip install --upgrade pip
#   .venv/bin/python3 -m pip install -r requirements.txt
#
# LUU Y: phai dung dtype=torch.bfloat16 (KHONG dung float16) cho model thi giac: float16 tran
# so (inf/nan) trong attention cua vision tower tren MPS.

# --- Model thi giac (Qwen2.5-VL-3B-Instruct qua transformers) ---
qwen-vl-utils
torchvision

# --- Model khop kich ban (Qwen3-Reranker qua transformers + compressed-tensors) ---
torch
transformers
accelerate
compressed-tensors
huggingface_hub
safetensors

# --- Xu ly anh / video ---
# KHONG cai opencv-python o day. Ca opencv-python lan av deu bundle mot ban ffmpeg rieng
# (libavdevice 61 vs 62); nap ca hai vao cung mot tien trinh thi runtime ObjC cua macOS bao
# lop AVFFrameReceiver/AVFAudioReceiver bi dang ky trung, kem canh bao "mysterious crashes".
# av la thu khong bo duoc (qwen_vl_utils doc video qua torchvision -> pyav), nen phan video
# cua ung dung doc thang bang av.
av
numpy
pillow

# --- Giao dien web ---
fastapi
uvicorn
pydantic
