# Bo thu vien cua RIENG sidecar nhung da phuong thuc (.venv-embed).
#
# TUYET DOI KHONG cai bo nay vao .venv chinh: transformers>=4.57 va torch 2.8 se lam
# vo AutoAWQ, keo theo Qwen2.5-VL-7B-AWQ khong chay duoc nua (xem requirements.txt goc).
# Hai venv song song la co y, khong phai trung lap nham.
#
# CACH CAI (hoac chay setup_embedding_sidecar.bat):
#
#   python -m venv .venv-embed
#   .venv-embed\Scripts\python.exe -m pip install --upgrade pip wheel
#   .venv-embed\Scripts\python.exe -m pip install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cu128
#   .venv-embed\Scripts\python.exe -m pip install -r embedding_sidecar\requirements.txt

# Da kiem chung chay tot voi transformers 5.17.0 + torch 2.8.0+cu128 tren RTX 4070 Ti SUPER.
# De ngo >= thay vi ghim cung: script chinh chu cua model (scripts/qwen3_vl_embedding.py)
# duoc tai ve theo dung phien ban repo nen van khop khi transformers len doi moi.
transformers>=4.57.0
qwen-vl-utils>=0.0.14
accelerate>=1.0.0
huggingface_hub>=0.36.0
pillow>=10.0.0
av>=13.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0
